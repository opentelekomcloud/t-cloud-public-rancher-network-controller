---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: tcloudclusternetworks.infrastructure.otc.t-systems.com
spec:
  group: infrastructure.otc.t-systems.com
  names:
    kind: TCloudClusterNetwork
    listKind: TCloudClusterNetworkList
    plural: tcloudclusternetworks
    shortNames:
    - tcn
    singular: tcloudclusternetwork
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.managementPolicy
      name: Policy
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.resources.vpc.id
      name: VPC
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              clusterRef:
                properties:
                  name:
                    minLength: 1
                    type: string
                  namespace:
                    minLength: 1
                    type: string
                required:
                - name
                - namespace
                type: object
              credentialSecretRef:
                properties:
                  name:
                    minLength: 1
                    type: string
                  namespace:
                    minLength: 1
                    type: string
                required:
                - name
                - namespace
                type: object
              endpointType:
                default: public
                enum:
                - public
                - internal
                - admin
                - publicURL
                - internalURL
                - adminURL
                type: string
              managementPolicy:
                default: Managed
                enum:
                - Managed
                - Observe
                - Abandon
                type: string
              network:
                properties:
                  securityGroup:
                    properties:
                      cni:
                        enum:
                        - canal
                        - flannel
                        type: string
                      id:
                        type: string
                      name:
                        type: string
                      sshAllowedCIDRs:
                        items:
                          format: cidr
                          type: string
                        minItems: 1
                        type: array
                    type: object
                  subnet:
                    properties:
                      availabilityZone:
                        type: string
                      cidr:
                        format: cidr
                        type: string
                      dnsNameservers:
                        items:
                          format: ipv4
                          type: string
                        type: array
                      gatewayIP:
                        format: ipv4
                        type: string
                      id:
                        type: string
                      name:
                        type: string
                    type: object
                  vpc:
                    properties:
                      cidr:
                        format: cidr
                        type: string
                      id:
                        type: string
                      name:
                        type: string
                    type: object
                required:
                - securityGroup
                - subnet
                - vpc
                type: object
              orphanCleanupAfter:
                default: 1h
                type: string
              projectName:
                type: string
              region:
                minLength: 1
                type: string
            required:
            - clusterRef
            - credentialSecretRef
            - network
            - region
            type: object
            x-kubernetes-validations:
            - message: Managed policy requires VPC CIDR, subnet CIDR, gatewayIP, and
                at least one SSH allowed CIDR
              rule: self.managementPolicy != 'Managed' || (has(self.network.vpc.cidr)
                && has(self.network.subnet.cidr) && has(self.network.subnet.gatewayIP)
                && size(self.network.securityGroup.sshAllowedCIDRs) > 0)
            - message: Observe policy requires VPC, subnet, and security-group IDs
              rule: self.managementPolicy != 'Observe' || (has(self.network.vpc.id)
                && has(self.network.subnet.id) && has(self.network.securityGroup.id))
            - message: clusterRef is immutable
              rule: self.clusterRef == oldSelf.clusterRef
            - message: region is immutable
              rule: self.region == oldSelf.region
            - message: projectName is immutable
              rule: self.projectName == oldSelf.projectName
          status:
            properties:
              clusterUID:
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
              ownershipToken:
                type: string
              resources:
                properties:
                  securityGroup:
                    properties:
                      controllerManaged:
                        type: boolean
                      id:
                        type: string
                      name:
                        type: string
                    type: object
                  subnet:
                    properties:
                      controllerManaged:
                        type: boolean
                      id:
                        type: string
                      name:
                        type: string
                    type: object
                  vpc:
                    properties:
                      controllerManaged:
                        type: boolean
                      id:
                        type: string
                      name:
                        type: string
                    type: object
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
